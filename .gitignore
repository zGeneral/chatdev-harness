# Demo app build artifacts (the company builds these; keep harness config separate)
demo/__pycache__/
demo/.pytest_cache/
demo/**/__pycache__/
**/*.pyc
.pytest_cache/
__pycache__/

# Secrets & local config — NEVER commit (only .env.example is tracked)
.env
.dev.vars
*.secret
secret.txt

# Cloudflare worker local state / deps
cloudflare/**/node_modules/
**/.wrangler/

# Scratch / OS
.DS_Store
*.log
.venv/
